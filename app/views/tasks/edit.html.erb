<h1>タスク編集</h1>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '編集' %>
<% end %>

<%= link_to 'タスク詳細ページに戻る', task_path(@task) %>